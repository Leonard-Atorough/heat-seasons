[build]
  command = "npm run build -w shared && npm run build -w frontend"
  publish = "packages/frontend/dist"
  base = "/"

[build.environment]
  NODE_ENV = ""

# SPA Routing: redirect all non-file routes to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
