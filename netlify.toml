[build]
  command = "npm run build"
  publish = "packages/frontend/dist"
  base = "/"

[build.environment]
  NODE_ENV = ""
  VITE_API_URL = "http://heat-seasons.up.railway.app"

# SPA Routing: redirect all non-file routes to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
